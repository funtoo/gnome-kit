diff --git a/src/gtd-task.c b/src/gtd-task.c
index 389f5ad..ef4dd2a 100644
--- a/src/gtd-task.c
+++ b/src/gtd-task.c
@@ -778,7 +778,6 @@ gtd_task_set_complete (GtdTask  *task,
           dt->minute = g_date_time_get_minute (now);
           dt->second = g_date_time_get_seconds (now);
           dt->is_date = 0;
-          dt->is_utc = 1;
 
           /* convert timezone
            *
